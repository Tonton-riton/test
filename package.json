{
  "name": "app-bar",
  "version": "1.0.0",
  "description": "BarAtlas - Application mondiale de découverte et recensement de bars",
  "scripts": {
    "dev": "concurrently -n \"API,CLIENT\" -c \"magenta,cyan\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
